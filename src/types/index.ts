export interface TUser {
  id: string
  name: string
  email: string
  image: string
  // Add other user properties
}

export interface TQueryParam {
  // Add query parameter types
}

export interface TResponseRedux {
  // Add redux response types
}
